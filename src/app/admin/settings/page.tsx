import { SettingsOverview } from '@/components/admin/settings/SettingsOverview';

export const metadata = { title: 'Settings Center | Admin' };

export default function SettingsCenterLandingPage() {
  return <SettingsOverview />;
}
